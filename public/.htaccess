RewriteEngine On

# Forward all requests to the Nuxt SSR process running on port 3000
# This requires mod_proxy and mod_rewrite to be enabled on your Apache server
RewriteRule ^(.*)$ http://localhost:3000/$1 [P,L]
